import { HiddenSocialNumberPipe } from './hidden-social-number.pipe';

describe('HiddenSocialNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new HiddenSocialNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
